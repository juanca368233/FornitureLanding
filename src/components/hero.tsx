import '../styles/hero.css'


// Creamos el componente como tal: Hero section

const HeroSection = () => {
    return (
        <section className='HeroBg'>
            <div>
              <h1>Donde la Innovación y la calidad se encuentran.</h1>  
            </div>
        </section>
    )
}

export default HeroSection